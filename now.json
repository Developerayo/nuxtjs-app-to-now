{
  "version": 2,
  "name": "nuxt-app",
  "builds": [
    { "src": "package.json", "use": "@now/static-build" }
  ]
}
  ],
  "routes": [
    { "src": "/_nuxt/.+", "headers": { "cache-control": "s-maxage=31536000" } },
    { "src": "/(.*)", "dest": "/" }
  ]
}
